# HTTP (your local FastAPI)
PPB_HTTP_BASE=http://127.0.0.1:8008
PPB_HTTP_PATH=/search
PPB_HTTP_MODE=io 
PPB_HTTP_IO_DELAY_MS=20 

# Postgres (async)
# Example: postgresql://pritesh-jha@/ppb
PPB_PG_DSN_ASYNC=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Optional: if your DSN uses peer auth on local socket, you can use:
# PPB_PG_DSN_ASYNC=postgresql://pritesh-jha@/ppb
